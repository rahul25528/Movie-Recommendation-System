# static/

This folder stores plots generated by the Jupyter notebook:

- `eda_plots.png` — EDA dashboard (rating distribution, movies per year, top genres, vote counts)
- `similarity_heatmap.png` — Cosine similarity heatmap between sample movies

Run the notebook to generate these files:
```bash
jupyter notebook notebooks/Movie_Recommendation_System.ipynb
```
